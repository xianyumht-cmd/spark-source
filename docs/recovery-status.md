# 源码恢复状态

## 已完成

- [x] 输入文件校验与哈希固定；
- [x] PE 头、数据目录和节表解析；
- [x] 节熵与磁盘缺失节确认；
- [x] 静态导入表恢复；
- [x] 资源目录完整枚举；
- [x] Manifest 与图标资源提取；
- [x] 入口桩、公共调度器定位；
- [x] 可复现分析脚本整理；
- [x] 第一阶段伪代码与数据结构重建。

## 尚未完成

- [ ] 获得装载并解密后的 `.text/.rdata/.data` 内存镜像；
- [ ] 定位真实 OEP；
- [ ] 重建完整 IAT；
- [ ] 对虚拟机指令集建立 opcode → 语义映射；
- [ ] 生成去虚拟化 CFG；
- [ ] 将本机代码转换为结构化伪代码；
- [ ] 按窗体、事件、模块重新组织为可编译工程。

## 完整恢复所需的下一阶段

必须在隔离的 32 位 Windows 分析环境中，于保护层完成初始化后采集进程内存。至少需要：

1. 进程完整内存转储；
2. 每个已提交可执行内存区的基址、大小和保护属性；
3. 到达原始入口点时的寄存器与线程上下文；
4. 解密后的导入地址表；
5. 虚拟机调度器执行轨迹。

拿到上述数据后，才能继续完成 OEP/IAT 修复、虚拟指令语义归纳和高级源码重建。仅对当前磁盘文件重复运行普通反编译器不会产生完整源码。
